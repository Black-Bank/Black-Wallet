export type optionType = {
  headerStyle: {
    backgroundColor: string;
  };
  headerTitleStyle: {
    color: string;
  };
  headerTintColor: string;
  headerTransparent: boolean;
  headerTitle: string;
};
